# Shaadime

A minimal Next.js application with template boilerplate removed.

## Run locally

```bash
npm install
npm run dev
```

Then open http://localhost:3000 in your browser.

## Where to edit

- Update the UI in `app/page.tsx`
- Add global styles in `app/globals.css`
- Change metadata in `app/layout.tsx`
